# AGENTS.md

> 完整约束在 [`GEMINI.md`](./GEMINI.md)，冲突时以它为准。本文件只是入口。

## 五条铁律速览

1. **禁止版本号通胀** —— 用户没说"发版"就不要动 `versionName`。
2. **禁止假发版** —— 不许只改 `version` 号就为未实现该功能的模块（尤其 `:desktop`）打包发布。
3. **安全性不可交换** —— 官方源优先于第三方代理；动态 dex 必须验签；解压必须防 Zip Slip。
4. **禁止静默吞异常** —— 不许空 catch；涉及用户数据的失败必须给可见提示。
5. **改前先读，交付前自检，汇报说实话** —— 验收标准有几条就报几条。附则：**禁止改验收工具**。

## 每次任务只读这三样（省下的上下文留给代码）

1. `GEMINI.md`
2. `docs/EVOLUTION_PREFERENCES.md`（用户喜好 + 已否决方向）
3. `docs/debt/<编号>.md` —— **本次任务那一张卡**，不要读整份索引

按需再读：`docs/DEVELOPMENT_GUIDE.md` §3（改 UI/数据层时）、
`docs/GEMINI_RATIONALE.md`（想知道某条规则为什么存在时）。

## 交付前必跑

```powershell
.\tools\selfcheck.ps1
```

输出原样贴进汇报（含指纹），**不要只贴通过项**。
脚本还会列出「还剩哪些模块没补文档」「哪些布局硬编码颜色最多」——
**这些状态由脚本现算，不要靠记忆或上一轮对话**。

汇报格式照抄 `GEMINI.md` §4 的模板，每节都要填。
