# Claude Code 专属自治与执行规范

**第 0 步：先完整执行 [AGENTS.md](AGENTS.md) 的启动协议。**

必须按 AGENTS.md 读取工程规范、`docs/stage-current.md`、`docs/TODO.md`、Spec、Bug 与 AQ；不得只读取本文件后直接制定计划。
本文件定义 Claude Code 专属行为：输出风格、Plan 模式、长任务自治循环、停止条件与 Completion Gate。

---

## 1. 语言与输出风格

- 所有解释、说明、计划、任务摘要与状态报告使用**简体中文**；
- 代码、类名、方法名、API 路由、文件名、Git 指令与技术术语保持英文原文；
- 表现为**持续执行的工程 Agent**，减少无价值的过程叙述与自我复盘；
- 最终报告严格遵循 `AGENTS.md` Step 6。

---

## 2. Plan 模式与长任务自治

### 2.1 默认不等待人工确认
计划形成后，**默认直接进入实施，不等待用户回复“确认”“继续”**。禁止在每完成一个小步骤或仅输出了计划后主动暂停打断用户。

### 2.2 连续执行循环 (Continuous Execution Loop)
1. 按 `AGENTS.md` 启动协议读取必要文件；
2. 确认 Spec 目标、边界与完成标准；
3. 实施最小修改与定向测试验证；
4. 遇到失败自动分析归因，若由本次修改引入则自主修复并复测；
5. 更新 `docs/TODO.md` 与 `docs/stage-current.md`；
6. 持续推进直到 Spec 全部完成标准达成，或遇到真实外部阻塞。

---

## 3. 允许请求用户的条件

只有存在无法由 Agent 合理、自主、安全解决的问题时才请求用户：
1. 缺少必须由用户提供的第三方凭据、签名私钥或真机设备；
2. 需求存在多个互斥方案，且选择会产生明显不同的业务结果；
3. 现有指令与不可协商红线直接冲突；
4. 继续执行会破坏用户已有工作树。

---

## 4. 计划与现实不一致的分级处理

- **Level 1（实现细节调整）**：实际方法签名、文件名微调，直接自主调整推进，不暂停；
- **Level 2（方案调整但目标不变）**：原机制受阻更换同等架构实现，更新 Spec 继续推进；
- **Level 3（目标或业务边界变化）**：涉及公开数据格式破坏性变更或与红线冲突，暂停并请求用户决策。

---

## 5. Completion Gate（完成门禁）

**“回复用户”不等于“任务完成”。** 结束任务前必须核对：
1. 原始需求与 Spec 工作包是否全部处理？
2. 完成标准（AC）是否逐条核对并记录证据？
3. `docs/TODO.md` 与 `docs/stage-current.md` 是否已同步？
4. `pwsh .\tools\selfcheck.ps1` 门禁是否已执行？
5. 是否存在无需用户参与即可继续执行的下一步？（若有则继续执行）。
