### 🌟 Collecter v2.2.4 更新日志 (Changelog)

#### 🚀 核心新特性与视觉动效重构：

1. **🎨 告别输入框文字穿模与原生粗糙边框**：
   - 彻底废弃导致浮动标签与输入内容重叠遮挡的旧式布局，采用自研暗色质感输入框（`bg_input_box`）与独立清晰标题。
   - 输入框支持翡翠绿外发光聚焦点亮效果，行高间距细致调优，任何屏幕下绝不穿模。

2. **✨ 记一笔 / 编辑记录 全量升级为高定卡片弹窗**：
   - 移除 Android 系统自带丑陋的原生 `buttonPanel` 灰色底框。
   - 配备 24dp 优雅大圆角卡片、顶栏专属图标与快捷关闭（✕）按钮。
   - 步进器升级为大号粗体触感加减按钮（`48dp x 48dp`）。
   - 底部双操作区配备 14dp 圆角高质感按钮（【取消】+【确认添加/保存修改】），100% 显眼防误触。

3. **🌊 全系统微动效与灵动跟手触控引擎**：
   - **弹窗动效**：所有弹窗新增专属 `CustomDialogAnimation`（轻盈平滑缩放淡入淡出）。
   - **触控回弹**：为全应用按钮、底部导航、分类药丸、步进器、历史列表卡片注入 `0.92f - 0.96f` 真实触感回弹。
   - **转场平滑**：底部导航（首页/生活流/报表/我的）新增丝滑淡入淡出页面转场。
