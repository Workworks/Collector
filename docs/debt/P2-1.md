# P2-1　清理布局硬编码颜色

**状态**：✅ 已完成　**优先级**：P2

## 现状

全量 69 个 XML 布局文件中的非合理例外硬编码颜色已全部治理完毕并替换为语义色，**「其余 N 处」清零（0 处）**。

- `activity_scanner.xml`：保留 8 处相机取景全黑遮罩与纯白指示（合理例外，已添加架构注释说明）。
- 其余 85 处硬编码颜色（含 `dialog_add_entry.xml`、`widget_expiring_sub.xml`、`widget_quick_add.xml` 等）已全量替换为 `@color/white`、`@color/card`、`@color/danger`、`@color/warning`、`@color/text_primary` 等语义色。

## 验收标准

- [x] `selfcheck` 第 3 项「其余 N 处」降至 0（实测：共 8 处，scanner 8 处，其余 0 处，判定为 ✅ 通过）
- [x] 保留的例外均有注释说明原因（`activity_scanner.xml` 顶部已添加架构注释）
- [x] 深色与浅色模式下小组件与各弹窗均适配语义色，无不可读文字

