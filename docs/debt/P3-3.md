# P3-3　业务逻辑测试覆盖

**状态**：✅ 已完成　**优先级**：P3

## 现状

从原本仅覆盖 2 条安全不变量（11 个用例），扩展至 21 个高覆盖率单元测试用例，覆盖全部核心业务纯逻辑。

## 覆盖用例清单

1. `VersionCompareTest.kt`：覆盖 `UpdateManager.isNewerVersion()`（常规三段升级、大小写 v 前缀兼容、同版本与降级、不同分段长度）；
2. `AssetDepreciationTest.kt`：覆盖闲置变现回血 ROI 统计（总投入、回血总额、净支出、回血率）与在役/订阅多态筛选；
3. `AssetExpiryAndThresholdTest.kt`：覆盖耗材安全低库存预警、耐用资产定期维保周期推算、实物外借逾期判定；
4. `BackupCodecTest.kt`：覆盖全量资产与分类 JSON 导出与安全导入往返无损一致性。

## 验收标准

- [x] 上述 4 类逻辑各有测试覆盖（新增 10 个业务逻辑测试用例，总数达 21 个）
- [x] 新增测试全部通过，且做过变异验证（把被测逻辑改坏，确认测试会变红）
- [x] `selfcheck.ps1` 单元测试判定全绿通过

