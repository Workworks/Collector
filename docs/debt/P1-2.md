# P1-2　ARCHITECTURE.md 模块补录

**状态**：✅ 已完成（未记录 0 个）　**优先级**：P1

## 现状

`app/src/main/java/com/kfaino/diapertracker/` 下全部 67 个 `.kt` 模块已 100% 记录至 `docs/ARCHITECTURE.md` 模块表，**未记录数为 0**。

## 验收标准

- [x] 本批新增记录的模块数 ≥ 10（第 3 批补录 15 个）
- [x] `selfcheck` 第 6 项的「未记录数」相比上一批**严格下降**（15 → 0）
- [x] 新增每一行保持三列格式，且职责描述与真实代码相符
- [x] （总目标）全部批次做完后未记录数为 0

## 进度

- ✅ 第 1 批（12 个）：五大收纳馆 + 数字资产 + 订阅管理 + 装箱舱 + 时光胶囊 + 外借 + `SmartIntakeHelper` + `ClipboardOrderBridge`。40 → 28
- ✅ 第 2 批（12 个）：教程三件套 + `LedgerManager` + `LanSyncHelper` + `NfcHelper` + `BluetoothPrinterHelper` + `InventoryAuditDialog` + `LocationHistoryDialog` + `RoomManagerDialog` + `CategoryManagerDialog` + `ResaleCopilotHelper`。28 → 16
- ✅ 第 3 批（15 个）：`MaintenanceManagerDialog` + `AiConciergeHelper` + 6 个核心 Adapter + `HotUpdateDialog` + `IdentityWatermarkHelper` + 2 个 Poster 生成引擎 + `ModernDatePickerDialog` + `ModernDialogHelper` + `PhotoPreviewDialog` + `ReminderReceiver`。16 → 0，全面清零！

