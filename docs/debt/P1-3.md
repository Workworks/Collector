# P1-3　更新架构图

**状态**：✅ 已完成　**优先级**：P1

## 现状

`ARCHITECTURE.md` 顶部的 mermaid 架构图已全面升级，清晰呈现六大核心分层与数据交互关系：
1. **UI 视图与交互层**：核心页面 Fragments、八大收纳馆族与场景舱、智能工具与引导体系、桌面小组件；
2. **核心控制与适配层**：主控制器 `MainActivity`、列表适配器族、智能引擎助手族；
3. **核心数据管理层**：数据门面 `DataStore`、多账本隔离 `LedgerManager`、收纳馆持久化仓储集 `VaultRepositories`、数据模型契约 `DataModels`；
4. **隐私安全与合规护栏**：生物识别锁、证照防盗流水印、官方源优先 `UpdateSource`（`api.github.com` 置顶）、补丁防路径穿越与 `SHA256withRSA` 签名验签；
5. **同步与多通道分发**：WebDAV 私有云同步、局域网免装 Web 互传、增量热更新与 Excel 兼容 CSV 导出；
6. **本地离线私有沙盒存储**：`SharedPreferences` & JSON、应用私有沙盒图片库、系统媒体库隔离目录。

## 验收标准

- [x] 图中出现上述六块（UI 层 / 控制适配层 / 数据管理层 / 安全合规层 / 同步分发层 / 本地沙盒存储层）
- [x] 图中每个具名模块都真实存在于代码中

