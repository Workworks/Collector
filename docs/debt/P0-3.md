# P0-3　消除静默吞异常

**状态**：✅ 已完成（2026-08-20）　**优先级**：P0

## 结果

全仓库 10 处空 catch 已全部补齐日志，实测 `selfcheck` 第 4 项为 0 处。
日志文案带上下文（如 `"保存图片至沙盒私有目录失败: $sourceUri"`）。

## 一处更正

初版把 `DataStore:296/587` 与 `ImageVaultHelper` 标成 🔴「用户数据静默丢失」，**这个判断是错的**：
- `DataStore:296` 是桌面小组件刷新（展示层）；
- `DataStore:587` 是分类 JSON 解析（有默认兜底）；
- `ImageVaultHelper` 失败返回 null，调用方 `MainActivity.kt:50/60` **本来就有 Toast**。

补日志就是正确且充分的处理。

## 保留的良性例外

`BluetoothPrinterHelper.kt` 的 `finally { socket?.close() }`、`ViewExt.kt` 的震动失败 ——
**带理由的忽略是对的写法**，`selfcheck.ps1` 已将其排除。
