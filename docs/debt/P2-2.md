# P2-2　抽象 8 个雷同的收纳馆弹窗

**状态**：✅ 已完成　**优先级**：P2

## 现状与治理结果

`FoodVaultDialog`、`HonorVaultDialog`、`FamilyVaultDialog`、`VoucherVaultDialog`、
`FamilyMedicineDialog`、`DigitalAssetManagerDialog`、`LendingManagerDialog`、`LifeCapsuleDialog`
8 大收纳馆中的通用交互与动效已全面下沉至 [`VaultUiHelper.kt`](../../app/src/main/java/com/kfaino/diapertracker/VaultUiHelper.kt)：
1. **统一视窗动效与透明底色**：`VaultUiHelper.setupVaultWindow(dialog)` 统一管理微动效与沉浸式背景；
2. **统一文本脱敏复制与即时反馈**：`VaultUiHelper.copyToClipboard(...)` 封装安全剪贴板与友好 Toast；
3. **统一搜索监听解耦**：`VaultUiHelper.bindSearchWatcher(...)` 统一快速绑定 TextWatcher；
4. **统一现代化日期选择器**：`VaultUiHelper.showDatePicker(...)` 封装 Calendar 往返转换与标准格式化；
5. **保留各馆专属类型安全 Binding**：不破坏各馆强类型 ViewBinding 与定制表单布局。

## 验收标准

- [x] ✅ 8 个馆的行为与重构前完全一致，统一使用 VaultUiHelper 消除模板冗余
- [x] ✅ 新增收纳馆时直接复用 VaultUiHelper 统一基建，代码量显著精简

