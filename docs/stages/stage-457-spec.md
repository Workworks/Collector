# Stage 457：整合本地与远端、修复安全与 WebDAV

用户授权整合全部本地/远端变动、提交并保持远端最新；本轮不创建新版本或 Release。

## 目标与边界

以远端 v4.3.6/main 与本地工作区共同形成整合树，保留远端新增模块/设置视口和本地 shared/备份/工作台/家庭功能。以 4.3.6/43 为现有版本基准，不另升版。保留包名、存储键、原签名外置配置；禁止将 keystore、业务数据、凭据、构建缓存提交。先保存本地快照，再整合；不覆盖丢弃用户工作。

## 工作包与 AC

| AC | 完成条件 | 验证 |
| --- | --- | --- |
| AC-01 | 双方实现整合、无冲突标记，不回退远端新增功能 | Git 三方对比、编译、双方新增测试 |
| AC-02 | Android WebDAV 用 HEAD，双端上传/下载及条件写入保留 | 单元/隔离协议与可用实际客户端测试 |
| AC-03 | DEX 在安装及启动前强验签，缺钥/缺签名/篡改拒绝；ZIP 不越界 | 安全负向回归，保留既有测试断言 |
| AC-04 | 构建/回归及 selfcheck 实测，文档反映当前整合状态 | Gradle 与原自检，不改脚本伪造通过 |
| AC-05 | 提交不含秘密，推送非强制；本地 HEAD 与 origin/main 一致 | 暂存审计、push、ls-remote/status |

设备/Native 安装与正式发布仍按独立门禁，不以源码同步冒充已发布修复。远端有新增提交时先重新整合验证，不 force push。

## 验收回填（2026-09-01）

- AC-01 PASS：三端编译/单测通过；远端新增文件无缺失，存储键差异已迁移并在 API 34 实测。
- AC-02 PASS：公网隔离协议、Android 实际 Helper、桌面往返及 Android→桌面恢复通过。
- AC-03 PASS：RSA 正/负向、ZIP 跨平台边界和体积限制通过；安装/启动调用均保留。
- AC-04 PASS：原自检 6/6；常规 114 项中 112 通过、2 项 opt-in 跳过，公网专项另 2 项均通过。
- AC-05：执行暂存审计与非强制推送；最终提交/远端一致性以 Git 记录及交付回复为准，不以报告里的预测哈希充当证据。

原始验证和整合取舍见 [交付报告](stage-457-report.md)。设备范围为模拟器，不冒充物理设备验收。
