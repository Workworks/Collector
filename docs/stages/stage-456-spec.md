# Stage 456：待办收敛与升级准备

用户授权完成当前待办并在完成后升级版本，提供仓库外签名 `F:\LANShare\debug.keystore`。实施前规格，2026-08-31。

## 目标与边界

按 P0→P1→P2 推进：恢复原签名构建、验证有数据的升级；补工作台/家庭/性能自动化和可用界面验证；修复附件残留；补备份运维与隐私/重试能力；重查 Native 和固定公网前置。保留包名与已有未提交修改，不把密钥、口令、业务数据写入仓库，不卸载旧版绕过升级。不默认启用公网或后台自动覆盖。

版本仅在对应交付内容和门禁核实后调整；真机、域名、凭据、工具链或长时运行缺少证据时明确 BLOCKED，不把模拟器等同真机。不得冒充所有任务已完成。隐私方案避免未经验证的全量数据迁移；先实现有明确测试边界的保护。

## 工作包与验收

| 编号 | 工作包 / 完成标准 | 验证 |
| --- | --- | --- |
| AC-01 | 仓库外签名与旧版匹配，release 显式配置且不能静默退回其他签名 | keytool、公钥指纹、APK 签名门禁；不输出私钥 |
| AC-02 | 原 APK 有数据安装后覆盖升级，原数据仍在且新入口可用 | 隔离模拟器；物理设备单独记录 |
| AC-03 | 工作台关键操作、家庭角色/过期/重授权、取消与失败安全 | 新增有意义的仓储/HTTP/设备测试，实际 UI 可用时记录 |
| AC-04 | 大备份边界、固定样本与持续操作性能有数据 | 定向压力测试，记录规模和耗时，不推断无限长期 |
| AC-05 | 失败附件导入不留下新孤立副本、不删除原引用附件 | 失败回滚与既有文件保护回归 |
| AC-06 | 备份归档与容量检查可执行，隐私/重试方案有安全边界 | 隔离目录演练；不得将演练当生产监控部署 |
| AC-07 | Native 安装与公网重查，存在前置则执行，不存在则给出具体证据 | 实际工具探测、构建/安装/连通性，禁复用旧产物冒充 |
| AC-08 | 最终版本、回归、自检和交付状态一致，账本逐项更新 | Gradle、selfcheck、发布报告；不以升版本修饰未验收项 |

本规格覆盖账本全部 11 项的推进；不能执行的外部验收保留在账本，不静默删除或降级为已完成。

验收回填见 [报告](stage-456-report.md)：AC-01/05 PASS；AC-02/03/04/06/08 PARTIAL；AC-07 BLOCKED。原签名已解决，全部待办与完整版本交付没有完成。

实测补充：AC-03 包含 Android cleartext 策略修复。因桌面家庭服务提供可信 LAN HTTP，Android 显式允许明文传输，家庭客户端代码只允许 localhost、loopback 和私有 IPv4 字面地址；公网必须 HTTPS。平台全局明文开关不是主机白名单，各带凭据传输仍需单独验证，不据此声称全应用已强制 HTTPS。
