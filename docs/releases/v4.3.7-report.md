# v4.3.7 发布验收报告

日期：2026-09-01。候选来自 Stage 457 完成后的 main；旧版基准为 GitHub Release v4.3.6 的真实 APK。

## 发布内容

- Android：`com.kfaino.diapertracker`，4.3.7 / versionCode 44，原签名 APK。
- Desktop：4.3.7 Universal JVM JAR，需要 Java 17；不是 Windows Native 安装器。
- Release 说明：[release_notes_v4.3.7.md](../../release_notes_v4.3.7.md)。

## 验收结果

| 门禁 | 结果 |
| --- | --- |
| clean 三端门禁 | `shared:test`、`desktop:test`、`app:testReleaseUnitTest`、`assembleRelease`、desktop JAR 全部通过；Android 66 项 0 失败 |
| 原 selfcheck | 6/6：版本三处 4.3.7、非相机硬编码色 0、空 catch 0、186 个 Android 模块均有记录；脚本指纹 `297194F2DEB2` |
| APK 身份 | `com.kfaino.diapertracker`，versionCode 44，versionName 4.3.7 |
| 签名连续性 | v4.3.6 与候选证书 SHA-256 均为 `03d73e71eb13c82a820dc50f4ef780630aa698591944693ff6b7c1c3c3bb013a` |
| 覆盖升级 | 一次性 API 34 AVD 安装真实 GitHub v4.3.6，探针写入中文数据；`adb install -r` 候选后读取相同数据、确认 44/4.3.7并清理，通过 |
| 启动 | 升级后冷启动返回 `Status: ok`，应用进程存在，最近 AndroidRuntime 日志无本包 FATAL |
| Android APK | SHA-256 `02c3c36abd95477a2b5ee2b70170a6b3e90cf8a2b70fe148f209f8731083e539` |
| Desktop JAR | SHA-256 `7885346f4089501f7dabfaa4442d52de1ef1afe2d7d5cf24697ac33d3d83979e`；已执行 desktop 测试，不冒充 Native 包 |

AC-01 至 AC-05 全部 PASS。发布提交为 `7da2c0390018cb2aaccb1b1f3f223cd4e3b6facd`；带注释 tag `v4.3.7` 解引用到该提交。正式 Release 于 2026-09-01T01:08:39Z 发布：[Collecter v4.3.7](https://github.com/Workworks/Collector/releases/tag/v4.3.7)。Release 非草稿、非预发布，包含且仅包含 Android APK 与 Desktop Universal JAR。重新下载后的两个 SHA-256 与候选完全一致，GitHub API asset digest 也一致。

原始构建、自检、验签、升级及测试证据位于 `docs/releases/evidence-4.3.7-final/`。

## 已知边界

- 发布设备验证使用 Android API 34 模拟器，没有物理设备。
- DEX 热补丁因未配置受信公钥而保持 fail-closed；本 Release 不上传补丁 ZIP。
- Quick Tunnel 仍是临时 WebDAV 验收入口，不是固定生产服务。
- 远端新增实验模块的存在不等于所有硬件及端到端能力均已验收。
