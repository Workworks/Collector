# GEMINI.md — Collecter 项目最高约束

> 本文件优先级**高于**单次对话里的临时口头指令（"快一点""顺手发个版"）。
> 临时指令可以扩大任务范围，但不能豁免任何一条铁律。冲突时先指出，等用户确认再执行。
>
> 每条规则背后都有真实事故。想知道某条为什么存在 → 读 `docs/GEMINI_RATIONALE.md`（**只在需要时读**，不要每次都读）。

---

## 1. 项目事实

| 项 | 值 |
| :--- | :--- |
| 应用名 | **Collecter**（资产与收纳管家） |
| Android 包名 | `com.kfaino.diapertracker` ⚠️ 历史遗留，**禁止改**（改了用户升级丢全部数据） |
| `rootProject.name` | `DiaperTracker` ⚠️ 同上，禁止"顺手改成 Collecter" |
| 版本真相源 | `app/build.gradle.kts` 的 `versionName`（README、文档、发布说明都对齐它） |
| 数据层 | SharedPreferences + 手写 JSON。无数据库、无 ORM、无后端 |
| 网络用途 | 仅版本更新 / 热补丁 / WebDAV / 局域网互传。业务数据 100% 离线 |
| 模块 | `:app`（Android 主力）、`:desktop`（Swing，**功能子集，与移动端数据格式不通**） |

---

## 2. 五条铁律（违反任意一条 = 本次任务失败）

### 🔴 1. 禁止版本号通胀

- 只有用户明确说"发版 / release / 打包发布"时，才允许改 `versionName` / `versionCode`。**"加个功能" ≠ "发个版"**。
- 单次任务最多 +1 个 patch 位。minor 位需用户同意，major 位**永远**需用户批准。
- 一天内不得发布超过 1 个 minor 版本。
- 版本号是给用户的承诺，不是任务完成度的计分板。

### 🔴 2. 禁止假发版

只改某模块的 `version` 号、但该模块并未实现本次功能，却仍为它打包发布 —— 这叫假发版。

- 本次功能没在 `:desktop` 落地 → **不打包、不改它的 version**，并在发布说明写明「Android 端独占」。
- 用户要求"全平台"而桌面端做不到 → **说出来**，给出改造清单让用户决定。不允许用改版本号假装做到。

### 🔴 3. 安全性不可交换

App 里存着用户**全家的身份证、护照、房产证、保单和加密密码**。以下为不可协商的不变量：

1. **官方源优先**：`api.github.com` / `github.com` 必须排候选列表第一位，第三方代理（ghfast / ghproxy / mirror）只能作 fallback。慢是体验问题，投毒是安全问题，不可交换。
2. **动态代码必须验签**：`DexClassLoader` 加载的任何 dex 必须先验签，失败即拒绝并回滚。**MD5 是指纹不是验签**。
3. **解压必须防路径穿越**：见 `PatchArchive.safeUnzip()`。
4. 不得以"修复超时 / 加速 / 先跑起来"为由降低上述任何一条。有冲突就停下来问。

> `app/src/test/` 下的 `UpdateSourceTest`、`PatchArchiveTest` 是这些不变量的回归护栏。
> **禁止删改测试断言让构建变绿。** 它们变红 = 你破坏了一条不变量，去改代码。

### 🔴 4. 禁止静默吞异常

- 禁止 `catch (_: Exception) {}` 这类空捕获（**只写一行 `// Ignore` 也算**）。
- 每个 catch 至少 `Log.w(TAG, "具体在做什么失败了", e)`，文案要带上下文（文件名 / URI / 原始值）。
- 涉及用户数据读写的失败，必须给**用户可见反馈**（Toast / 对话框）。
- 合理的忽略（如 `finally { socket.close() }`）要写明为什么可以忽略。

### 🔴 5. 改动前先读，交付前先自检，汇报时说实话

- 必读见 §3 Step 0，必跑见 §4。
- **没做到的必须主动说。** 不许把"编译通过"说成"功能已验证"。跳过了某项就写"未验证 XXX"。
- **验收标准有几条，汇报里就必须出现几条**，每条带 ✅通过 / ❌未通过 / ⬜未达成（附原因）。少一条就是违反，无论结论对不对。
- **附则：验收工具不可动。** `tools/selfcheck.ps1` 与测试是「证据的来源」，不是「要交付的成果」。禁止为让某项变绿或预填答案而修改它们。确需修改先取得用户同意，并在汇报里写明原因。脚本会打印自身指纹，指纹变了而汇报没说明 = 违反本条。

---

## 3. 标准流程

### Step 0：动手前必读

1. 本文件；
2. `docs/EVOLUTION_PREFERENCES.md` —— 用户喜好与**已否决方向负向清单**（绝不重复提已否决方向）；
3. 本次任务对应的**任务卡**：`docs/debt/<编号>.md`（如 `docs/debt/P1-2.md`）。
   ⚠️ **每次都重新打开读，不要凭记忆、不要从上一轮对话里抄** —— 任务卡会被更新，验收标准可能已经变了。
4. 与本次任务直接相关的源文件（**完整读，不要只读片段就改**）。

> `docs/DEVELOPMENT_GUIDE.md` §3 是代码规范细则，改 UI / 数据层时再读，不必每次读。

### Step 1：影响面盘点（写第一行代码前先说清楚）

- 会新增/修改哪些文件；
- 是否触及 `DataStore` JSON 序列化（→ §5 数据兼容性义务）；
- 是否需要同步 `TutorialDialog.kt` / `InteractiveGuideTour.kt`（→ §6）；
- 桌面端是否跟进（做不到就明说）；
- 是否触及铁律 3 的安全敏感路径。

### Step 2：实现

**优先复用**（`ModernDialogHelper`、`ViewExt`、`ImageVaultHelper`），不要复制粘贴出第 9 个雷同的 Vault 弹窗。

### Step 3 & 4：自检与汇报

见 §4。

---

## 4. Definition of Done

**一条命令跑完全部检查：**

```powershell
.\tools\selfcheck.ps1
```

- 输出是一段可直接粘贴的 Markdown，**原样贴进汇报，不要只贴通过项**，包含开头那行指纹。
- exit code 1 不代表脚本坏了，是提示「有项目需要你在汇报里解释」。
- `-SkipBuild` 仅供改动过程中快速自查；**交付必须跑完整版**，否则汇报里写明"未验证编译与测试"。

**汇报照抄这个模板，每节都要填：**

```markdown
## 做了什么
- （逐个文件说明）

## 验证到什么程度
（粘贴 selfcheck 完整输出，含指纹。另说明：是否在真机/模拟器实测过功能？
  "编译通过" ≠ "功能验证"，没实测就直言"仅编译验证"）

## 验收标准逐条对照
（**现在重新打开 docs/debt/<编号>.md**，把验收标准复制过来。
  不要从记忆或上一轮对话里抄 —— 标准会被更新。
  有几条抄几条，每条标 ✅ / ❌ / ⬜（附原因））

## 没做什么、为什么
- （跳过的检查、未跟进的平台、发现但没顺手改的问题）
- 桌面端：☐ 已跟进　☐ 未跟进，原因：______
```

---

## 5. 数据兼容性（优先级仅次于安全）

- 新增 JSON 字段，读取侧**必须**给安全默认值：`optString("img_p", "")`。
- **严禁**重命名或删除已有 JSON key，**严禁**改 `applicationId`。
- 改过序列化逻辑后，用旧版备份 JSON 走一次 `importBackupJson` 往返验证。
- 导出 CSV 必须带 `﻿` BOM 头。

---

## 6. 文档与教程同步义务

新增任何用户可见功能，以下**全部**必须同步：

1. `TutorialDialog.kt` —— 新增 `TutorialItem`（icon / title / summary / steps / tip / tourKey）；
2. `InteractiveGuideTour.kt` —— 跟手演练，支持中途随时退出；
3. 演练若注入示例数据，**退出时必须原样全部清除**，绝不污染真实数据；
4. `docs/ARCHITECTURE.md` 模块表 + `docs/USER_MANUAL.md` 使用章节；
5. `docs/CHANGELOG.md` 仅在实际发版时写。

---

## 7. 禁止清单

**产品方向**（来自 `EVOLUTION_PREFERENCES.md`，重复提出即失败）：
❌ 外部智能家居/IoT　❌ 纯售后/工单/电话簿　❌ 重度 3D CAD 建模　❌ 底层硬件极客体检

**工程行为**：
❌ 未经要求改版本号或发版
❌ 为没实现功能的模块打包发布
❌ 用"编译通过"代替"功能验证"
❌ 空 catch 吞异常　❌ 硬编码颜色/尺寸
❌ 改 `applicationId` / `rootProject.name`
❌ 删除或重命名已有 JSON key
❌ 降低任何一条安全不变量
❌ 汇报里省略没做到的部分
❌ 改 `selfcheck.ps1` 或测试断言让检查变绿 / 预填答案
❌ 汇报里给出的改动理由与实际改动不符
❌ 声称"原样抄录"某文件，实际抄的是记忆或上一轮对话

---

## 8. 停下来问，不要发明

以下情况**必须问用户**：需求有多种合理解读且实现差异大；需新增第三方依赖；需改数据结构 / 序列化格式 / 包名；需在安全性与速度之间取舍；用户要"全平台"但桌面端成本高；用户指令与本文件冲突。

**一次好的提问，比十次自作主张的返工便宜得多。**
